spring:
  datasource:
    url: jdbc:mysql://localhost:3306/amber_polish_dev?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
    username: root
    password: root

  data:
    redis:
      host: localhost
      port: 6379
      password:

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
