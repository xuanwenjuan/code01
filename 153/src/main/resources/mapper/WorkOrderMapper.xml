<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liquor.brewing.mapper.WorkOrderMapper">

    <resultMap id="WorkOrderMap" type="com.liquor.brewing.entity.WorkOrder">
        <id column="id" property="id"/>
        <result column="order_no" property="orderNo"/>
        <result column="order_name" property="orderName"/>
        <result column="formula_id" property="formulaId"/>
        <result column="category_id" property="categoryId"/>
        <result column="plan_quantity" property="planQuantity"/>
        <result column="actual_quantity" property="actualQuantity"/>
        <result column="unit" property="unit"/>
        <result column="priority" property="priority"/>
        <result column="status" property="status"/>
        <result column="brewer_id" property="brewerId"/>
        <result column="supervisor_id" property="supervisorId"/>
        <result column="inspector_id" property="inspectorId"/>
        <result column="plan_start_date" property="planStartDate"/>
        <result column="plan_end_date" property="planEndDate"/>
        <result column="actual_start_time" property="actualStartTime"/>
        <result column="actual_end_time" property="actualEndTime"/>
        <result column="remark" property="remark"/>
        <result column="formula_name" property="formulaName"/>
        <result column="category_name" property="categoryName"/>
        <result column="brewer_name" property="brewerName"/>
        <result column="supervisor_name" property="supervisorName"/>
        <result column="inspector_name" property="inspectorName"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <select id="selectWorkOrderPage" resultMap="WorkOrderMap">
        SELECT w.*, f.formula_name, c.category_name,
               u1.real_name as brewer_name, u2.real_name as supervisor_name, u3.real_name as inspector_name
        FROM work_order w
        LEFT JOIN liquor_formula f ON w.formula_id = f.id
        LEFT JOIN liquor_category c ON w.category_id = c.id
        LEFT JOIN sys_user u1 ON w.brewer_id = u1.id
        LEFT JOIN sys_user u2 ON w.supervisor_id = u2.id
        LEFT JOIN sys_user u3 ON w.inspector_id = u3.id
        ${ew.customSqlSegment}
        ORDER BY w.priority DESC, w.create_time DESC
    </select>

</mapper>
