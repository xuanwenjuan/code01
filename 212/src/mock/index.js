import { users } from './users'
import { waybills, statusList, exceptionTypes } from './waybills'
import trackingRecords from './trackingRecords'

export const mockData = {
  users,
  waybills,
  trackingRecords,
  statusList,
  exceptionTypes
}

export default mockData
