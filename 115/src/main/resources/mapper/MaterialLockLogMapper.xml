<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zongshi.brush.mapper.MaterialLockLogMapper">

    <select id="selectByOrderId" resultType="com.zongshi.brush.entity.MaterialLockLog">
        SELECT * FROM material_lock_log WHERE order_id = #{orderId} ORDER BY create_time DESC
    </select>

    <select id="selectByMaterialId" resultType="com.zongshi.brush.entity.MaterialLockLog">
        SELECT * FROM material_lock_log WHERE material_id = #{materialId} AND lock_status = 1 ORDER BY create_time DESC
    </select>

</mapper>
