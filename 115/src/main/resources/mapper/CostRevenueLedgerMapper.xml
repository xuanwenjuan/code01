<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zongshi.brush.mapper.CostRevenueLedgerMapper">

    <select id="selectTotalMaterialCost" resultType="java.math.BigDecimal">
        SELECT SUM(material_cost) FROM cost_revenue_ledger
        WHERE is_deleted = 0
        <if test="startDate != null">
            AND ledger_date >= #{startDate}
        </if>
        <if test="endDate != null">
            AND ledger_date <= #{endDate}
        </if>
    </select>

    <select id="selectTotalLaborCost" resultType="java.math.BigDecimal">
        SELECT SUM(labor_cost) FROM cost_revenue_ledger
        WHERE is_deleted = 0
        <if test="startDate != null">
            AND ledger_date >= #{startDate}
        </if>
        <if test="endDate != null">
            AND ledger_date <= #{endDate}
        </if>
    </select>

    <select id="selectTotalRevenue" resultType="java.math.BigDecimal">
        SELECT SUM(sales_revenue) FROM cost_revenue_ledger
        WHERE is_deleted = 0
        <if test="startDate != null">
            AND ledger_date >= #{startDate}
        </if>
        <if test="endDate != null">
            AND ledger_date <= #{endDate}
        </if>
    </select>

    <select id="selectTotalProductionQuantity" resultType="java.lang.Integer">
        SELECT SUM(production_quantity) FROM cost_revenue_ledger
        WHERE is_deleted = 0
        <if test="startDate != null">
            AND ledger_date >= #{startDate}
        </if>
        <if test="endDate != null">
            AND ledger_date <= #{endDate}
        </if>
    </select>

</mapper>
