<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fishing.distribution.mapper.RevenueItemMapper">

    <select id="selectByCondition" resultType="com.fishing.distribution.entity.RevenueItem">
        SELECT * FROM revenue_item
        <where>
            deleted = 0
            <if test="itemType != null and itemType != ''">
                AND item_type = #{itemType}
            </if>
            <if test="itemCategory != null and itemCategory != ''">
                AND item_category = #{itemCategory}
            </if>
            <if test="startTime != null">
                AND create_time &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                AND create_time &lt;= #{endTime}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="selectByOrderId" resultType="com.fishing.distribution.entity.RevenueItem">
        SELECT * FROM revenue_item
        WHERE order_id = #{orderId}
        AND deleted = 0
        ORDER BY create_time DESC
    </select>

</mapper>
