<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fishing.distribution.mapper.RevenueStatisticsMapper">

    <select id="selectByDateRange" resultType="com.fishing.distribution.entity.RevenueStatistics">
        SELECT * FROM revenue_statistics
        WHERE statistics_date BETWEEN #{startDate} AND #{endDate}
        AND statistics_type = #{statisticsType}
        AND deleted = 0
        ORDER BY statistics_date DESC
    </select>

</mapper>
